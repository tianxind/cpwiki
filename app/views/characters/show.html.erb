<p class="subtitle">角色信息<span class="edit"><%= link_to 'Edit', edit_character_path(@character) %></span></p>

<div class="character_basic_info">
	<%= render(:partial => "character_info", :locals => {:character => @character}) %>
</div>
<div>
<%= formatted(@character.summary) %>
</div>

